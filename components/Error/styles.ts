import { Card } from '@material-ui/core';
import styled from 'styled-components';

const StyledCard = styled(Card)`
  min-height: 80%;
  font-size: 1.5rem;

  .text {
    margin: 0 0 2rem;
  }
`;

export { StyledCard };
